# Crosstab

### Run Button
in our today's discussion, we have decided not to auto run when clicking on save.  the reason being, if user is having an intent to save, anyways, the run wouldn't be displayed, and uh, User will probably go back. So we have decided not to do that. instead, we are making that run as a mandatory before user can save, so which means, let's say user adds a new variable into column row. Currently, the run button gets enabled and the save button which be disabled because the cross tab is not yet run.  and if user tries to hover on the save, then we show the tool tip indicating that please run the cross tab before saving it.  and in case, if user clicks back without saving, we show a new dialogue of indicating that your cross tab is not saved, please run the cross trip and save it, or discard it in the confirmation dialogue. So that would be the difference in the other way, if a user has run but not saved, we already have a confirmation dialogue, which just says URC changes are not saved. Do you want to save or not? So the run is a new change that we have over here.

### New Variable Creation - Crosstab
 for new variable creation, we have decided that instead of having set output to at individual value level, it will be at a variable level, because it makes no sense logically to have set output to at value level and showing error at the end.  and also, it doesn't make sense for user to create an variable, which can have set output to as text and output to as variable and numeric 3 differently. That doesn't make any sense logically. and we also decided to only have a numeric and text as an output for now, a variable will focus it, focus on it if it's needed after the initial release.  and finally, we are moving the set output to at an variable level setting, instead of at value level setting, so that would be changing the UI, and we would be using the numeric and text as an. 2 subtypes which we'll be going forward with. (yesterdays notes)